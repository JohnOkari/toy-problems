

function arrayInput(myArray){
    let newArray = [];
    for (i=0; i < myArray; i++){
        newArray.push[myArray[i]];
        return newArray
    }
    newArray.sort(function(a,b){return a - b});
}       

console.log((myArray))